{
  // Defines the connection string for the default database connection.
  // This connection string is set to use a SQLite database with the file name 'Sqlite.db'.
  "ConnectionStrings": {
    "DefaultConnection": "Data Source=Sqlite.db"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
